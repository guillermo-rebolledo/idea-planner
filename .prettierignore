node_modules
out
dist
test-results
playwright-report
coverage
pnpm-lock.yaml

# Local editor/agent state.
.claude

# Agent-managed issue tracker prose: the skills that write these files own
# their layout, and reformatting only churns them.
.scratch

# Impeccable critique snapshots and config: archive prose owned by the skill.
.impeccable

# Generated from the installed Codex binary, and regenerated by
# `pnpm codex:protocol` rather than edited (`docs/agents/codex-protocol.md`).
app/src/core/harness/codex-protocol

# Vendored from assistant-ui's shadcn registry, then trimmed by hand to its
# presentational parts. Kept close to its source so a newer copy can be diffed
# against it rather than reconciled by eye (ticket 13).
app/src/renderer/src/components/ui/model-selector.tsx
