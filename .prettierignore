node_modules
out
dist
test-results
playwright-report
coverage
pnpm-lock.yaml

# Local editor/agent state.
.claude

# Agent-managed issue tracker prose: the skills that write these files own
# their layout, and reformatting only churns them.
.scratch

# Generated from the installed Codex binary, and regenerated by
# `pnpm codex:protocol` rather than edited (`docs/agents/codex-protocol.md`).
app/src/core/harness/codex-protocol

# Vendored from assistant-ui's shadcn registry and kept close to its source so
# a newer copy can be diffed against it (ticket 13).
app/src/renderer/src/components/ui/model-selector.tsx
