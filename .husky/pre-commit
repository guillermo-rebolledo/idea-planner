# Fast gate: format and lint what is staged, then prove the types and the
# Core behavior still hold. The heavy build-and-launch suites run on push.
pnpm lint-staged
pnpm --filter argos-desktop typecheck
pnpm --filter argos-desktop test:core
